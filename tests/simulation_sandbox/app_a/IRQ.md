---
id: IRQ-001
recipient: Doer
implementing_actor: Doer
priority: high
---

# Task Overview
Create a file named `success.txt` with the content `AGENT_WORKS` in the root of the `app_a` directory.

# Scope of Work
- [ ] Create `success.txt` in the root of `app_a`.
- [ ] Set the content of `success.txt` to `AGENT_WORKS`.

# Out of Scope
- Modifying `GEMINI.md` or any other existing files.
- Creating any other files or directories.

# Architectural Constraints (Project Invariants)
- Follow the project-specific rituals defined in `GEMINI.md`.

# Definition of Done
- `success.txt` exists in `app_a` and contains `AGENT_WORKS`.
